# pnpm is the primary package manager — see pnpm-workspace.yaml for minimumReleaseAge.
# This file is kept as a safety net if someone runs npm directly.
# Requires npm >= 11.10.0. Incompatible with the `before` config key.
min-release-age=3
